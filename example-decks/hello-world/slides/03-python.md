# Python Execution

This slide runs **real Python** in your browser using Pyodide (CPython compiled to WebAssembly).

## Capabilities

- Full Python 3 standard library
- Install packages like `pandas`, `numpy`, `matplotlib`
- No server required — runs entirely in the browser

> Note: First load may take a few seconds while the Python runtime initializes.
