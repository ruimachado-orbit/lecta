# SQL Execution

Run SQL queries against an **in-memory SQLite database**.

## How it works

- Uses `sql.js` (SQLite compiled to WebAssembly)
- Seed data is loaded automatically from `seed.sql`
- Query results display as formatted tables

This is perfect for demonstrating database concepts without needing a running database server.
