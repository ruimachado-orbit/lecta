# Native Execution

This slide runs code using your **local Node.js installation** via `child_process`.

## When to use native execution

- Run any language installed on your machine
- Access the file system, network, etc.
- Use for demos that need real system interaction

> Native execution runs with your local permissions. Use for trusted code only.
